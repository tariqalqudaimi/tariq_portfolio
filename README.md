# ملف شخصي وسيرة ذاتية ديناميكي | Dynamic Portfolio & CV Website

هذا المشروع هو عبارة عن موقع شخصي (Portfolio) وسيرة ذاتية (CV) متكامل، تم تطويره باستخدام PHP و MySQL لإدارة المحتوى بشكل ديناميكي. يتميز المشروع بوجود واجهتين مختلفتين يمكن للمستخدم التبديل بينهما بسهولة: واجهة موقع كاملة، وواجهة سيرة ذاتية مدمجة وأنيقة.

This is a comprehensive Personal Portfolio and CV website developed using PHP and MySQL for dynamic content management. The project features two distinct layouts that users can switch between seamlessly: a full portfolio website layout and a compact, elegant CV layout.

---

### ✨ الميزات الرئيسية | Key Features

*   **✌️ واجهتان في مشروع واحد:**
    *   **واجهة الموقع الكامل:** تصميم احترافي لعرض الخدمات، المهارات، الخبرات، المشاريع، والشهادات بشكل مفصل.
    *   **واجهة السيرة الذاتية:** تصميم مدمج يركز على عرض المعلومات الأساسية بطريقة تشبه السيرة الذاتية الكلاسيكية.
*   **🎨 نظام مظهرين (فاتح ومظلم):** يمكن للمستخدم التبديل بين الوضع الفاتح (Light Mode) والمظلم (Dark Mode) في كلا الواجهتين.
*   **🌐 ثنائي اللغة (عربي وإنجليزي):** دعم كامل للغتين العربية (RTL) والإنجليزية (LTR) مع إمكانية التبديل الفوري.
*   **💾 حفظ خيارات المستخدم:** يتم حفظ تفضيلات المستخدم (الواجهة، المظهر، اللغة) في المتصفح (`localStorage`) لزيارته التالية.
*   **🔧 إدارة المحتوى ديناميكيًا:** كل النصوص والصور والبيانات يتم جلبها من قاعدة بيانات MySQL، مما يسهل تحديث المحتوى دون لمس الكود.
*   **📱 تصميم متجاوب:** كلا الواجهتين متجاوبتان بالكامل وتعملان بشكل ممتاز على جميع الشاشات (موبايل، تابلت، ديسكتوب).

---

### 🛠️ التقنيات المستخدمة | Tech Stack

*   **الواجهة الأمامية (Frontend):**
    *   HTML5
    *   [Tailwind CSS](https://tailwindcss.com/)
    *   JavaScript (Vanilla JS)
    *   [Lucide Icons](https://lucide.dev/)
*   **الواجهة الخلفية (Backend):**
    *   PHP
*   **قاعدة البيانات (Database):**
    *   MySQL

---

### 🚀 كيفية التثبيت والتشغيل | Setup and Installation

لتشغيل هذا المشروع على جهازك المحلي، اتبع الخطوات التالية:

**1. المتطلبات الأساسية:**
*   تأكد من وجود خادم محلي مثل [XAMPP](https://www.apachefriends.org/index.html) أو [WAMP](https://www.wampserver.com/en/) أو MAMP مثبت على جهازك.

**2. تحميل المشروع:**
*   قم بنسخ (clone) المستودع إلى مجلد `htdocs` (في XAMPP) أو `www` (في WAMP):
    ```bash
    git clone https://github.com/your-username/your-repo-name.git
    ```

**3. إعداد قاعدة البيانات:**
*   قم بفتح `phpMyAdmin` من خلال الخادم المحلي (`http://localhost/phpmyadmin`).
*   أنشئ قاعدة بيانات جديدة وسمّها (على سبيل المثال `portfolio_db`).
*   اذهب إلى قاعدة البيانات الجديدة، واختر تبويب `Import`.
*   قم باستيراد ملف `database.sql` الموجود في المشروع لإنشاء جميع الجداول اللازمة.

**4. توصيل قاعدة البيانات:**
*   افتح ملف `config/database.php`.
*   قم بتعديل معلومات الاتصال لتطابق إعداداتك:
    ```php
    <?php
    $servername = "localhost";
    $username = "root"; // اسم المستخدم الافتراضي
    $password = ""; // كلمة المرور الافتراضية (عادة فارغة)
    $dbname = "portfolio_db"; // اسم قاعدة البيانات التي أنشأتها

    // Create connection
    $conn = new mysqli($servername, $username, $password, $dbname);

    // Check connection
    if ($conn->connect_error) {
        die("Connection failed: " . $conn->connect_error);
    }
    
    // Set charset to utf8mb4 to support Arabic
    $conn->set_charset("utf8mb4");
    ?>
    ```

**5. تشغيل المشروع:**
*   الآن يمكنك فتح المشروع في متصفحك عبر الرابط: `http://localhost/your-repo-name`

---

### 🗃️ هيكل قاعدة البيانات | Database Structure

قاعدة البيانات تحتوي على الجداول التالية لإدارة المحتوى:

*   `personal_info`: لتخزين المعلومات الشخصية (الاسم، الوظيفة، الإيميل، الصورة، ...).
*   `services`: لعرض الخدمات التي تقدمها.
*   `skills`: للمهارات التقنية.
*   `soft_skills`: للمهارات الشخصية.
*   `experience`: لتجارب العمل السابقة.
*   `projects`: لعرض المشاريع المنجزة.
*   `education`: للشهادات التعليمية.
*   `certificates`: للشهادات الاحترافية والدورات.

---
